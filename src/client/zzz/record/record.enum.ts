/**
 * An enum representing the schedule type for Forgotten Hall.
 * @readonly
 * @enum {number}
 */
export enum ShiyuDefenseScheduleEnum {
  /**
   * The current schedule.
   */
  CURRENT = 1,
  /**
   * The previous schedule.
   */
  PREVIOUS = 2,
}

/**
 * An enum representing the schedule type for Deadly Assault.
 * @readonly
 * @enum {number}
 */
export enum DeadlyAssaultScheduleEnum {
  /**
   * The current schedule.
   */
  CURRENT = 1,
  /**
   * The previous schedule.
   */
  PREVIOUS = 2,
}